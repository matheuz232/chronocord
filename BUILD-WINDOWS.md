# Build Windows

Recomendado: Node.js 22 LTS.

```powershell
npm install --include=optional
npm run diagnose
npm run check
npm run build
````

Para o instalador no Windows:

```powershell
npm run dist
```

Não inclua `node_modules` no ZIP de distribuição do código.
